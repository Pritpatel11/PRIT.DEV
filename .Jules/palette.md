## 2025-02-12 - Form Label Linkage
**Learning:** Relying solely on layout to link stylized labels to forms impairs accessibility for screen readers and touch target size for visual users.
**Action:** Ensure inputs are explicitly linked with `htmlFor` and `id`, and add a `cursor-pointer` to labels to signal clickability.

## 2024-08-06 - Form Label Accessibility and Micro-UX
**Learning:** Explicit `htmlFor` and `id` linkage on custom-styled form labels is critical for screen readers, and adding `cursor-pointer` to such labels improves micro-UX by visually signaling clickability.
**Action:** Always link labels and inputs explicitly and use `cursor-pointer` on labels for better user experience.

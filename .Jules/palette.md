## 2024-03-24 - Semantic Form Association
**Learning:** Explicitly linking form labels to inputs using `htmlFor` and `id` attributes is necessary for screen reader accessibility, and pairing this with a `cursor-pointer` on labels visually cues interactive clicking behavior.
**Action:** Always link form labels to their inputs (using `id` and `htmlFor`) and apply pointer styles in stylized forms without native browser defaults.

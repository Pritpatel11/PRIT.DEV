## 2026-08-16 - [Accessible Form Labels]
**Learning:** Explicitly linking form labels to inputs using `htmlFor` and `id` significantly improves keyboard and screen reader accessibility. Adding `cursor-pointer` makes it clear the label can be clicked to focus the input.
**Action:** Always ensure custom styled form inputs are explicitly linked to their corresponding labels.
